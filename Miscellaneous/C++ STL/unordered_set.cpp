#include <bits/stdc++.h>
using namespace std;

int main()
{
    unordered_set<int> st; // unique

    // lower_bound and upper_bound function does not works, rest all function are same as set

    // it does not stores in any particular order

    // it has a better complexity than set in most cases, except some when collision happens

    // most cases time complexity is O(1) e.g. insert, erase. in worst case o(n) but in a few scenarios only

    return 0;
}