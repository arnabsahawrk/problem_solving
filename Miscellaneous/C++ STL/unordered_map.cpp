#include <bits/stdc++.h>
using namespace std;

int main()
{
    unordered_map<int, int> mpp;

    // same as set unordered_set difference

    // it works in a constant time O(1) but in worse case it works O(n) that is once in a blue moon not always

    return 0;
}